# Traefik environment
export TRACE=0
export TRAEFIKENABLE=true
# TRAEFIKENABLE needs to be true first time service is started, and everytime a new hostname is added, 
# to ensure certs are generated - otherwise, can be disable after first start.

export TRAEFIK_HOST=<example.net>
export TRAEFIK_VER=2.10.7

# IP address for Docker Swarm to advertise
export IPADDR=<IP address swarm advertises on>

